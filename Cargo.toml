[package]
name = "matmul-workers"
version = "0.1.0"
edition = "2021"

[dependencies]
burn = { version = "0.16", features = ["ndarray", "wgpu"] }
crossbeam-channel = "0.5"
rand = "0.8"
num_cpus = "1.16"
